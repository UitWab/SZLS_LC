# 智能建梁数字孪生平台数据库模块 V3 测试结果交接文档

## 一 测试结论

- 测试日期：2026-09-07
- 时区：Asia/Shanghai
- 测试对象：`dev-db` 当前未提交 V3 工作区
- 测试结论：通过
- V3 定向测试：`25 passed`
- 全量回归测试：`129 passed`
- V3 最新迁移往返：通过
- Python 包构建：通过
- 缺陷：未发现
- 冻结建议：建议通过独立审计后冻结 V3 候选交付版本

本次测试独立验证了 V3 操作审计日志的只追加存储、查询、迁移、公开契约和打包结果，并完整回归 V1、V2。现有自动测试全部通过；自动套件中未直接覆盖的长度边界、同时间稳定排序和写入后异常回滚已通过不落盘补充测试验证，临时数据已清理。

## 二 测试基线

| 项目 | 实际值 |
| --- | --- |
| 项目目录 | `D:\Project\SZLS_LC\01.backend_db` |
| Git 分支 | `dev-db` |
| HEAD/V2 基线 | `cdd5d4307e595d7b112958250c7ffc9c879cc180` |
| Python | `3.11.9` |
| Pytest | `9.1.1` |
| MySQL | `8.0.46` |
| V3 包版本 | `3.0.0` |
| V3 数据契约版本 | `3.0.0` |
| V3 目标迁移 | `f8a2d4c6b901` |
| 测试库 | `smart_beam_twin_test` |
| 开发库 | `smart_beam_twin_dev` |

测试对象是当前工作区而不是仅测试 `HEAD`。测试开始前已记录 `git status --short`；未执行 `git reset`、`git checkout --`、`git clean`、提交或推送。

## 三 测试依据

已阅读并按以下资料执行：

1. `README.md`；
2. `docs/交接文档/V3测试任务交接文档.md`；
3. `docs/V3操作审计日志设计决策.md`；
4. `docs/交接文档/V3_B模块接入交付说明_2026-09-07.md`；
5. `docs/A-B接口接入说明.md` 中 V3 审计接口；
6. `D:\Project\SZLS_LC\00.docs\数据库数据字典\智能建梁数字孪生平台_V3_数据库数据字典_操作审计基础域.md`；
7. `backend_db/tests/conftest.py`；
8. `backend_db/tests/test_operation_audit_log.py`。

测试安全配置会拒绝不以 `_test` 结尾的测试库。本次显式设置 `TEST_DB_NAME=smart_beam_twin_test` 和 `DB_NAME=smart_beam_twin_test`。

## 四 环境检查

| 检查项 | 结果 | 关键输出 |
| --- | --- | --- |
| Python 版本 | 通过 | `Python 3.11.9` |
| 依赖检查 | 通过 | `No broken requirements found.` |
| 源码编译 | 通过 | 退出码 `0`，无错误输出 |
| 测试库安全配置 | 通过 | 两个数据库变量均为 `smart_beam_twin_test` |

执行命令：

```powershell
.\.venv\Scripts\python.exe --version
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m compileall -q backend_db
```

## 五 自动测试结果

### 5.1 V3 定向测试

定向套件由审计日志、数据库结构、公开接口和架构边界测试组成：

```powershell
.\.venv\Scripts\pytest.exe -q `
  backend_db\tests\test_operation_audit_log.py `
  backend_db\tests\test_schema.py `
  backend_db\tests\test_interfaces.py `
  backend_db\tests\test_architecture.py
```

结果：

```text
25 passed in 0.52s
```

### 5.2 全量回归

首次全量结果：

```text
129 passed in 1.78s
```

完成迁移往返、包构建和补充验证后的最终结果：

```text
129 passed in 1.95s
```

本次没有失败、错误、跳过或预期失败用例。

## 六 V3 功能验证矩阵

| 能力 | 结论 | 验证证据 |
| --- | --- | --- |
| 表字段和可空规则 | 通过 | 13 个字段与数据字典一致；项目、用户、快照和可选详情字段可空；必填代码和时间非空 |
| 外键 | 通过 | `project_id -> project.id`、`actor_user_id -> app_user.id`，均为 `RESTRICT` |
| 索引 | 通过 | 发生时间、项目、操作用户、资源和请求 ID 共 5 个索引均存在 |
| DTO 长度和空白 | 通过 | 9 个字符串字段最大长度可用，超长和纯空白均被拒绝 |
| 额外字段 | 通过 | 额外敏感字段被 Pydantic 拒绝 |
| UTC 时间 | 通过 | 带时区时间转换为无时区 UTC；时间范围倒置被拒绝 |
| 系统级记录 | 通过 | 项目和用户均可空，记录可写入和读取 |
| 未登录失败记录 | 通过 | 仅提供操作人快照可记录 |
| 项目级记录 | 通过 | 项目编码和用户引用正确解析，未提供快照时采用用户显示名称 |
| 引用校验 | 通过 | 不存在项目和用户分别返回公共异常 |
| `record`、`get`、`list` | 通过 | 返回 DTO，不暴露 ORM；写入提交，读取和分页正常 |
| 多条件筛选 | 通过 | 项目、用户、结果、时间范围和关键词组合筛选通过 |
| 分页 | 通过 | 两页结果、总数、前后页标志正确；`include_total=False` 正确 |
| 稳定排序 | 通过 | 默认时间倒序；相同 `occurred_at` 时按 `id DESC` 稳定排序 |
| `request_id` 语义 | 通过 | 相同请求 ID 可保存多条记录，不具有唯一或幂等约束 |
| CRUD 事务 | 通过 | CRUD 只 flush、不 commit；上下文退出后数据回滚 |
| Service 事务 | 通过 | 成功写入提交；flush 后强制异常时数据完整回滚 |
| 只追加边界 | 通过 | 公开 Service 只有 `record/get/list`，无更新、启停或删除方法 |
| V1/V2 回归 | 通过 | 全量 129 项测试通过 |

## 七 补充验证

现有自动测试未直接断言以下三个边界，本次使用不落盘脚本在测试库补充验证：

### 7.1 字符串长度和空白

对以下 9 个字段分别验证最大长度、最大长度加一和纯空白：

- `project_code`：64；
- `actor_name`：128；
- `action_code`：64；
- `resource_type`：64；
- `resource_code`：128；
- `result_code`：32；
- `request_id`：128；
- `source`：32；
- `summary`：500。

结果：最大长度全部接受，超长和纯空白全部拒绝。

### 7.2 稳定排序和重复请求 ID

写入 3 条具有相同 `occurred_at` 和相同 `request_id` 的记录，默认查询返回顺序与 ID 倒序一致，且三条记录均成功保存。

结果：通过。

### 7.3 失败回滚

在审计记录完成 flush、生成 DTO 之前强制抛出异常，随后查询相同 `request_id` 的记录数。

结果：异常正常传播，记录数为 0，事务完整回滚。

补充验证完成后删除临时记录，最终检查 `manual-%` 请求记录数量为 0。

改进建议：将上述三项补充验证转为正式自动测试，以增强后续回归保护；该建议不阻塞本次 V3 交付。

## 八 迁移验证

### 8.1 初始一致性

- 测试库初始版本：`f8a2d4c6b901 (head)`；
- `alembic check`：`No new upgrade operations detected.`。

### 8.2 最新迁移往返

1. 测试库成功从 `f8a2d4c6b901` 降级到 `e5c1a7b3d902`；
2. 降级后确认 `operation_audit_log` 表不存在；
3. 成功升级回 `f8a2d4c6b901 (head)`；
4. 升级后审计表、13 个字段、主键、2 个外键和 5 个索引全部恢复；
5. 升级后 `alembic check` 无差异；
6. 升级后全量 129 项测试再次全部通过。

### 8.3 数据库最终状态

| 数据库 | 最终迁移 | 检查结果 | 写入说明 |
| --- | --- | --- | --- |
| `smart_beam_twin_test` | `f8a2d4c6b901 (head)` | 无迁移差异；补充测试残留为 0 | 执行测试数据事务和 V3 最新迁移往返 |
| `smart_beam_twin_dev` | `f8a2d4c6b901 (head)` | 无迁移差异 | 仅只读检查 |

开发库未执行 downgrade、清空、重建或其他写操作。

## 九 Python 包构建

执行命令：

```powershell
.\.venv\Scripts\python.exe -m pip wheel . --no-deps --no-build-isolation --wheel-dir .v3_test_wheel
```

| 项目 | 实际值 |
| --- | --- |
| 文件名 | `smart_beam_twin_backend_db-3.0.0-py3-none-any.whl` |
| 大小 | 75,798 字节 |
| SHA-256 | `c0a5544ef9cf2c964ae84ca61efcb8f91211ea622f4d09d39d7c550c131d9d97` |
| 构建结果 | 成功 |

已确认 wheel 包含：

- `backend_db/crud/operation_audit_log.py`；
- `backend_db/models/operation_audit_log.py`；
- `backend_db/schemas/operation_audit_log.py`；
- `backend_db/services/operation_audit_log.py`；
- `backend_db/migrations/versions/f8a2d4c6b901_add_operation_audit_log.py`。

本次创建的 `.v3_test_wheel` 临时目录已清理。

## 十 缺陷与风险

本次没有发现可复现的代码缺陷、测试缺陷、迁移缺陷、数据库状态缺陷、环境缺陷或包构建缺陷。

非阻塞改进项：把本次不落盘补充验证的长度边界、同时间稳定排序和 flush 后失败回滚加入正式自动测试。当前实现已通过这些场景，风险仅在于未来变更的自动回归保护不足。

## 十一 工作区状态

- 测试开始前已有 V3 未提交工作区改动，符合交接文档描述；
- 测试过程没有修改业务源码、迁移或既有测试；
- 测试生成的 `.v3_test_wheel` 已删除；
- 手工补充验证没有落盘脚本，测试数据已清理；
- 本文档是本次测试新增的交接产物；
- 未提交、未推送。

## 十二 冻结建议

V3 定向测试、全量回归、数据结构、公开契约、事务边界、迁移往返和包构建均已通过，未发现交付阻塞项。

建议完成独立代码审计后冻结 V3 候选交付版本。冻结操作应由主开发任务提交当前工作区并创建正式版本提交或标签，本测试任务不执行提交或推送。

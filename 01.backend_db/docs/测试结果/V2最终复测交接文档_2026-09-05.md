# 智能建梁数字孪生平台数据库模块 V2 最终复测交接文档

## 一 最终结论

- 测试日期：2026-09-05
- 时区：Asia/Shanghai
- 测试结论：通过
- 全量测试：`118 passed`
- V2 补缺测试：6 项，全部通过
- 迁移一致性及最新迁移往返：通过
- Python 包构建：通过
- 代码、测试、迁移、数据库或环境缺陷：未发现
- 建议：可以冻结 V2 候选交付版本

本次复测关闭了上一轮唯一遗留项 TQ-05“项目成员重复加入幂等未覆盖”。当前测试任务交接文档规定的功能矩阵均已有自动测试证据且全部通过。

## 二 测试基线

| 项目 | 实际值 |
| --- | --- |
| 项目目录 | `D:\Project\SZLS_LC\01.backend_db` |
| Git 分支 | `dev-db` |
| HEAD | `e1d9c798dfae4dcc33063e2274957787c98fc92e` |
| Python | `3.11.9` |
| Pytest | `9.1.1` |
| MySQL | `8.0.46` |
| 包版本 | `2.0.0` |
| 数据契约版本 | `2.0.0` |
| 目标迁移版本 | `e5c1a7b3d902` |
| 测试库 | `smart_beam_twin_test` |
| 开发库 | `smart_beam_twin_dev` |

测试对象是 `dev-db` 分支的当前未提交 V2 工作区，而不是仅测试 `HEAD`。测试过程未重置、提交、推送或覆盖原有工作区内容。

## 三 本轮新增覆盖

新增测试：

```text
test_add_project_member_is_idempotent_and_reactivates_existing_record
```

该测试验证：

1. 同一用户连续两次加入同一项目时，两次返回相同成员 ID；
2. 重复加入后数据库中仍只有一条 `project_member` 关系；
3. 已停用成员再次加入时复用原成员关系；
4. 再次加入会把原成员关系恢复为启用状态；
5. 重新启用后数据库仍只有一条成员关系。

测试通过，TQ-05 已关闭。

## 四 自动测试结果

测试库显式锁定为 `smart_beam_twin_test`：

```powershell
$env:TEST_DB_NAME = 'smart_beam_twin_test'
$env:DB_NAME = 'smart_beam_twin_test'
.\.venv\Scripts\pytest.exe -q
```

迁移往返前结果：

```text
118 passed in 1.41s
```

单独执行 V2 补缺测试文件：

```text
collected 6 items
6 passed in 0.59s
```

最新迁移降级、升级及包构建完成后的最终全量结果：

```text
118 passed in 1.41s
```

没有失败、错误、跳过或预期失败用例。

## 五 环境和基础检查

| 检查项 | 结果 |
| --- | --- |
| Python 版本 | `Python 3.11.9` |
| 依赖完整性 | `No broken requirements found.` |
| Python 源码编译 | 通过，退出码 `0` |
| 测试库保护 | `TEST_DB_NAME` 和 `DB_NAME` 均锁定为 `smart_beam_twin_test` |

## 六 迁移验证

### 6.1 初始一致性

- 测试库初始版本：`e5c1a7b3d902 (head)`；
- `alembic check`：`No new upgrade operations detected.`。

### 6.2 最新迁移往返

1. 测试库成功从 `e5c1a7b3d902` 降级到 `d2a8f41c6e90`；
2. 降级后确认 `process_definition` 表不存在；
3. 成功重新升级到 `e5c1a7b3d902 (head)`；
4. `process_definition` 表恢复；
5. `id` 主键恢复；
6. `process_code` 唯一约束恢复；
7. `project_id` 外键正确指向 `project.id`，删除规则为 `RESTRICT`；
8. `ix_process_definition_project_id` 索引恢复；
9. 升级后 `alembic check` 无差异；
10. 升级后 118 项测试再次全部通过。

### 6.3 开发库终态

- 开发库：`smart_beam_twin_dev`；
- 最终迁移：`e5c1a7b3d902 (head)`；
- `alembic check` 无差异；
- 开发库仅执行只读检查，未执行降级或其他写操作。

## 七 包构建

执行：

```powershell
.\.venv\Scripts\python.exe -m pip wheel . --no-deps --no-build-isolation --wheel-dir .v2_test_wheel
```

| 项目 | 实际值 |
| --- | --- |
| 文件名 | `smart_beam_twin_backend_db-2.0.0-py3-none-any.whl` |
| 大小 | 65,577 字节 |
| SHA-256 | `57b893155d2a25b73b5a1dc2bf4bcf6ee3c9646b71dd9a18ae7b06d8d41833d7` |
| 构建结果 | 成功 |

本次创建的 `.v2_test_wheel` 临时目录已清理，没有新增 Git 可见构建残留。

## 八 功能矩阵

| 模块 | 结论 | 覆盖摘要 |
| --- | --- | --- |
| 项目 | 通过 | 新建、读取、更新、分页筛选、排序、启停、编码唯一 |
| 工序 | 通过 | 通用/项目工序、合并查询、归属更新、排序、启停、编码唯一冲突 |
| 用户 | 通过 | DTO/凭据隔离、散列更新、资料更新、启停、筛选、排序和分页 |
| 角色权限 | 通过 | 目录增改查、分页筛选、排序、启停、授权撤权幂等 |
| 系统角色 | 通过 | 角色范围校验、分配撤销幂等和查询 |
| 项目成员 | 通过 | 加入、重复加入幂等、停用后重新加入、分页筛选、排序和启停 |
| 项目角色 | 通过 | 角色范围校验、分配撤销幂等 |
| 有效权限 | 通过 | 系统/项目权限合并去重；停用用户、角色、权限或成员后的排除逻辑 |
| V1 回归 | 通过 | 梁型、区域、梁位、梁 CRUD，事务回滚和并发占位规则 |
| 接口边界 | 通过 | 公开 Service/Protocol、DTO、无物理删除、ORM/CRUD/Session 隔离 |

## 九 历史问题关闭情况

| 编号 | 问题 | 最终状态 |
| --- | --- | --- |
| TQ-01 | 权限范围、幂等及停用链路覆盖不完整 | 已关闭，相关测试通过 |
| TQ-02 | 用户维护与分页覆盖不完整 | 已关闭，相关测试通过 |
| TQ-03 | 工序编码唯一冲突覆盖缺失 | 已关闭，相关测试通过 |
| TQ-04 | 目录和项目成员分页边界覆盖不完整 | 已关闭，相关测试通过 |
| TQ-05 | 项目成员重复加入幂等未覆盖 | 已关闭，新增测试通过 |

本轮没有新增缺陷或测试覆盖缺口。

## 十 最终状态和冻结建议

- 测试库最终为 `e5c1a7b3d902 (head)`；
- 开发库最终为 `e5c1a7b3d902 (head)`；
- 最新迁移往返已恢复；
- 自动测试、迁移检查和包构建全部通过；
- 临时构建目录已清理；
- 未修改源码、迁移或既有测试；
- 本文档是本轮新增的最终测试交接产物，前两份报告保留作为问题发现和关闭记录。

基于当前测试证据，建议冻结 V2 候选交付版本。冻结前应由主开发任务确认并提交当前未提交工作区，然后创建正式版本提交或标签；本测试任务不执行提交或推送。

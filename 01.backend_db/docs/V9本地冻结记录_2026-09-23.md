# V9 冻结记录

## 1. 冻结结论

V9 统一异常事项记录以 `9.0.0` 基线冻结，目标迁移为 `a9d3e5f7b102`，冻结标签为 `backend-db-v9.0.0`。用户于 2026-09-23 授权冻结并推送远端。

## 2. 冻结范围

- 梁场内异常事项的人工登记、查询、更新、解决、关闭和受控作废；
- 项目与梁作用域、异常分类、等级、来源、发生时间和责任信息；
- 外部幂等、项目停用并发保护、条件筛选、稳定页码分页和默认异常时间线；
- ORM、Alembic 迁移、DTO、CRUD、Service、公开 Protocol、组合工厂、测试、数据字典和 A-B 接入说明。

不包含 B 的 HTTP 接口、登录鉴权、岗位权限、页面、工单流程、通知推送、设备采集、自动异常识别、阈值计算或异常处置编排。

## 3. 冻结前验证

| 验证项 | 结果 |
|---|---|
| V9 专项、接口及结构测试 | `43 passed` |
| 完整自动测试 | `229 passed`，迁移往返前后各通过一次 |
| 项目停用与异常新建并发测试 | 连续 `20/20` 轮通过 |
| 开发库与测试库迁移 | `a9d3e5f7b102 (head)` |
| 模型与迁移一致性 | 两库均无差异 |
| 异常事项记录残留 | 开发库、测试库均为 `0` |
| 默认时间线索引执行计划 | 命中组合索引，无文件排序 |
| Wheel 构建与隔离导入 | `9.0.0` 通过 |
| Wheel SHA-256 | `7D14E2A6A19F61EEBABC7A215B11F367DF730821851CDBF2A777FD0FBD623EF7` |
| 依赖、源码编译和 Git 差异格式 | 通过 |

独立测试还完成了开发库、测试库从 V9 降级至 V8 后重新升级至 V9 的迁移往返验证，结论为通过。

## 4. 审计说明

独立审计未发现 P0～P2 级问题，确认共享锁顺序、项目停用并发保护、完整幂等判定、稳定分页、默认时间线索引、迁移和异常封装符合 V9 设计。审计提出的两项 P3 文档口径问题已在冻结提交中同步，本次冻结没有遗留阻塞问题。

## 5. 后续规则

- `backend-db-v9.0.0` 标签创建后不可移动；
- 冻结后发现的问题使用新提交、向前迁移和后续版本修复，不改写冻结标签；
- 后续版本不随 V9 自动启动，必须重新确认明确需求、设计边界并取得授权；
- A 继续只提供数据库模型、迁移、数据契约和服务能力，不开发 B 的应用层功能。

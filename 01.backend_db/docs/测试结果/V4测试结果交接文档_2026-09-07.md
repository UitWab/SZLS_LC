# 智能建梁数字孪生平台数据库模块 V4 测试结果交接文档

> **审计后更新（2026-09-14）**
>
> 后续独立审计复现了一个 MySQL `REPEATABLE-READ` 下的并发创建缺陷，
> 因此本文下方 2026-09-07 的“未发现缺陷”仅作为首次测试历史保留，
> 不再代表当前最终结论。现已将活动工单检查改为锁定当前读，并把并发交错、
> 重复编码、停用梁位、终态保护、无总数分页、稳定排序和 CRUD 不提交纳入自动回归。
> 修复后 V4 联合定向测试为 `32 passed`，全量回归为 `156 passed`；
> 确定性并发用例额外连续执行 10 次均通过。测试库和开发库均为
> `a4d7e9f2c105 (head)`，`alembic check` 均无模型差异。当前审计发现已修复，
> 已纳入本地 `backend-db-v4.0.0` 冻结版本，尚未推送。

## 一 测试结论

- 测试日期：2026-09-07
- 时区：Asia/Shanghai
- 测试对象：`dev-db` 当前未提交 V4 工作区
- 测试结论：通过
- V4 定向测试：`28 passed`
- 全量回归测试：`152 passed`
- V4 最新迁移往返：通过
- Python 包构建：通过
- 缺陷：未发现
- 冻结建议：建议通过独立代码审计后冻结 V4 候选交付版本

本次独立验证了 V4 梁位工单的数据库结构、DTO、三类工单生命周期、项目隔离、状态流转、事务和并发边界，并完整回归 V1 至 V3。现有自动测试全部通过；自动套件未直接断言的重复编码、停用梁位、终态二次流转、无总数分页、稳定排序和 CRUD 不提交等场景已通过不落盘补充测试验证，临时数据已清理。

## 二 测试基线

| 项目 | 实际值 |
| --- | --- |
| 项目目录 | `D:\Project\SZLS_LC\01.backend_db` |
| Git 分支 | `dev-db` |
| HEAD/V3 基线 | `c5c260a2ebe97b4087937429f00db429cb5ce085` |
| V3 标签 | `backend-db-v3.0.0` |
| Python | `3.11.9` |
| Pytest | `9.1.1` |
| MySQL | `8.0.46` |
| V4 包版本 | `4.0.0` |
| V4 数据契约版本 | `4.0.0` |
| V4 目标迁移 | `a4d7e9f2c105` |
| 测试库 | `smart_beam_twin_test` |
| 开发库 | `smart_beam_twin_dev` |

测试对象是当前 V4 工作区而不是仅测试 `HEAD`。测试开始前已保存 `git status --short`；未执行 `git reset`、`git checkout --`、`git clean`、提交或推送。

## 三 测试依据

已阅读并按以下资料执行：

1. `README.md`；
2. `docs/交接文档/V4测试任务交接文档.md`；
3. `docs/V4梁位工单设计决策.md`；
4. `docs/交接文档/V4_B模块接入交付说明_2026-09-07.md`；
5. `docs/A-B接口接入说明.md` 中 V4 梁位工单契约；
6. `D:\Project\SZLS_LC\00.docs\数据库数据字典\智能建梁数字孪生平台_V4_数据库数据字典_梁位工单基础域.md`；
7. `backend_db/tests/conftest.py`；
8. `backend_db/tests/test_beam_position_work_order.py`。

测试安全配置会拒绝不以 `_test` 结尾的测试库。本次显式设置 `TEST_DB_NAME=smart_beam_twin_test` 和 `DB_NAME=smart_beam_twin_test`。

## 四 环境检查

| 检查项 | 结果 | 关键输出 |
| --- | --- | --- |
| Python 版本 | 通过 | `Python 3.11.9` |
| 依赖检查 | 通过 | `No broken requirements found.` |
| 源码编译 | 通过 | 退出码 `0`，无错误输出 |
| 测试库安全配置 | 通过 | 两个数据库变量均为 `smart_beam_twin_test` |

执行命令：

```powershell
.\.venv\Scripts\python.exe --version
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -m compileall -q backend_db
```

## 五 自动测试结果

### 5.1 V4 定向测试

定向套件包含梁位工单、数据库结构、公开接口和架构边界测试：

```powershell
.\.venv\Scripts\pytest.exe -q `
  backend_db\tests\test_beam_position_work_order.py `
  backend_db\tests\test_schema.py `
  backend_db\tests\test_interfaces.py `
  backend_db\tests\test_architecture.py
```

结果：

```text
28 passed in 0.80s
```

### 5.2 全量回归

首次全量结果：

```text
152 passed in 1.92s
```

迁移往返、补充验证和包构建完成后的最终结果：

```text
152 passed in 1.94s
```

本次没有失败、错误、跳过或预期失败用例。

## 六 功能测试矩阵

| 必测能力 | 结论 | 验证证据 |
| --- | --- | --- |
| `PLACE` 生命周期 | 通过 | 创建为待执行、开始、完成；完成后梁进入目标梁位 |
| `MOVE` 生命周期 | 通过 | 保存原梁位快照；完成后原梁位释放、梁进入新梁位 |
| `RELEASE` 生命周期 | 通过 | 不接受目标梁位；完成后梁当前位置为空 |
| 类型形状 | 通过 | PLACE/MOVE 缺少目标、RELEASE 提供目标、未知类型均被拒绝 |
| 状态流转 | 通过 | 仅允许 PENDING 开始、未结束取消、IN_PROGRESS 完成 |
| 终态保护 | 通过 | COMPLETED/CANCELED 后开始、完成、取消均被拒绝 |
| 重复工单编码 | 通过 | 转换为稳定的 `ResourceAlreadyExistsError` |
| 项目隔离 | 通过 | 其他项目无法读取或列出工单；梁和梁位引用限制在同一项目 |
| 停用梁位 | 通过 | 停用目标梁位不能创建 PLACE/MOVE 工单 |
| 单梁单活动工单 | 通过 | 普通重复创建和并发创建均只允许一个未结束工单 |
| 创建不预占目标梁位 | 通过 | 创建 PLACE 后梁当前位置保持为空 |
| 目标被占用时回滚 | 通过 | 梁保留原梁位，工单保持 IN_PROGRESS |
| 原梁位变化时回滚 | 通过 | 完成被拒绝，工单保持 IN_PROGRESS，不覆盖梁新状态 |
| 取消后新建 | 通过 | 取消后允许为同一根梁创建新工单 |
| 分页和无总数分页 | 通过 | 页数据、前后页标志、`has_next` 和 `total=None` 正确 |
| 筛选和关键词 | 通过 | 项目、梁、类型、状态、计划时间和关键词组合查询正确 |
| 稳定排序 | 通过 | 相同 `planned_at` 时按 ID 作为稳定次序 |
| CRUD 事务 | 通过 | 创建只 flush 不 commit，退出未提交事务后记录不存在 |
| 状态字段白名单 | 通过 | CRUD 拒绝修改目标梁位等业务字段 |
| DTO 边界 | 通过 | 对外返回 Pydantic DTO，不暴露 ORM |
| 公开接口边界 | 通过 | 仅提供 create/get/get_by_code/list/start/complete/cancel，无任意 update/delete |
| V1 至 V3 回归 | 通过 | 全量 152 项测试通过 |

## 七 补充验证

现有自动测试未直接覆盖以下场景，本次使用不落盘脚本在测试库补充验证：

### 7.1 类型和资源校验

- MOVE 不提供目标梁位：拒绝；
- 未知工单类型：拒绝；
- 停用目标梁位：拒绝。

### 7.2 编码与终态

- 两根不同梁创建相同工单编码：第二次返回 `ResourceAlreadyExistsError`；
- CANCELED 工单再次开始、完成或取消：全部拒绝；
- COMPLETED 工单再次开始、完成或取消：全部拒绝。

### 7.3 查询和稳定排序

创建 3 条具有相同 `planned_at` 的已取消工单，通过项目、类型、状态、时间范围和关键词组合筛选，并使用 `include_total=False` 分两页读取。

结果：

- 第一页 2 条、第二页 1 条；
- `total` 均为 `None`；
- `has_next`、`has_previous` 正确；
- 相同计划时间下按照 ID 升序稳定返回。

### 7.4 CRUD 不提交

直接调用 `create_beam_position_work_order`，确认 flush 后生成 ID；退出未提交 Unit of Work 后重新查询，记录数为 0。

结果：通过。

补充验证结束后已清理测试数据。最终检查：

```text
wo_test_projects=0
wo_test_orders=0
```

改进建议：将以上补充场景转为正式自动测试，以增强后续版本的回归保护；该建议不阻塞本次 V4 交付。

## 八 数据库结构与迁移

### 8.1 表结构

`beam_position_work_order` 的 14 个字段与数据字典一致：

- 必填：`id`、`work_order_code`、`order_type`、`beam_id`、`status`、`created_at`、`updated_at`；
- 可空：`project_id`、`source_position_id`、`target_position_id`、`planned_at`、`started_at`、`finished_at`、`remark`；
- 主键：`id`；
- 唯一约束：`work_order_code`；
- 外键：项目、梁、原梁位和目标梁位共 4 个，均为 `RESTRICT`；
- 业务组合索引：`(project_id, status, id)` 和 `(beam_id, status)`；
- MySQL 还为两个梁位外键建立了辅助索引。

### 8.2 迁移往返

1. 测试库初始为 `a4d7e9f2c105 (head)`；
2. `alembic check` 无差异；
3. 成功降级到 `f8a2d4c6b901`；
4. 降级后 `beam_position_work_order` 不存在；
5. 成功升级回 `a4d7e9f2c105 (head)`；
6. 升级后 14 字段、主键、唯一约束、4 外键和 2 组合索引恢复；
7. 恢复后 `alembic check` 无差异；
8. 恢复后全量 152 项测试再次通过。

### 8.3 最终数据库状态

| 数据库 | 最终迁移 | 检查结果 | 写入说明 |
| --- | --- | --- | --- |
| `smart_beam_twin_test` | `a4d7e9f2c105 (head)` | 无迁移差异；V4 测试残留为 0 | 执行测试数据事务和 V4 迁移往返 |
| `smart_beam_twin_dev` | `a4d7e9f2c105 (head)` | 无迁移差异 | 仅只读检查 |

开发库未执行 downgrade、清空、重建或其他写操作。

## 九 Python 包构建

执行命令：

```powershell
.\.venv\Scripts\python.exe -m pip wheel . --no-deps --no-build-isolation --wheel-dir .v4_test_wheel
```

| 项目 | 实际值 |
| --- | --- |
| 文件名 | `smart_beam_twin_backend_db-4.0.0-py3-none-any.whl` |
| 大小 | 85,337 字节 |
| SHA-256 | `c09d1f9a53717645695ad96b5fa5e0a7d177f328889194796f50ad975b2c894d` |
| 构建结果 | 成功 |

已确认 wheel 包含：

- `backend_db/crud/beam_position_work_order.py`；
- `backend_db/models/beam_position_work_order.py`；
- `backend_db/schemas/beam_position_work_order.py`；
- `backend_db/services/beam_position_work_order.py`；
- `backend_db/services/_beam_positioning.py`；
- `backend_db/migrations/versions/a4d7e9f2c105_add_beam_position_work_order.py`。

本次创建的 `.v4_test_wheel` 临时目录已清理。

## 十 缺陷与风险

本次没有发现可复现的代码缺陷、测试缺陷、迁移缺陷、数据库状态缺陷、环境缺陷或包构建缺陷。

非阻塞改进项：把本次补充验证的重复编码、停用梁位、终态保护、无总数分页、稳定排序和 CRUD 不提交加入正式自动测试。当前实现已通过这些场景，风险只在于未来修改时的自动回归保护不足。

测试通过不能替代独立代码审计；并发锁顺序、异常映射和项目隔离实现仍应由 V4 审计任务复核。

## 十一 工作区状态

- 测试开始前已有 V4 未提交工作区改动，符合交接文档描述；
- 测试前工作区包含 13 项已修改状态和 13 项未跟踪状态条目；
- 测试过程没有修改业务源码、迁移或既有测试；
- 测试生成的 `.v4_test_wheel` 已删除；
- 补充验证没有落盘脚本，临时数据已清理；
- 本文档是本次测试新增的交接产物；
- 未提交、未推送。

## 十二 冻结建议

V4 定向测试、全量回归、数据库结构、公开契约、工单状态机、并发和事务边界、迁移往返及包构建均已通过，未发现交付阻塞项。

建议完成独立代码审计后冻结 V4 候选交付版本。冻结操作应由主开发任务提交当前工作区并创建正式版本提交或标签，本测试任务不执行提交或推送。

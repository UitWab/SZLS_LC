# 智能建梁数字孪生平台 V1 数据库数据字典 梁场基础域

- **版本：** V1.1 正式版
- **数据库：** MySQL 8.0.x
- **ORM 与迁移：** SQLAlchemy 2.x 与 Alembic
- **当前范围：** `yard_area`、`beam_type`、`beam_position`、`beam`
- **基础单位：** 长度和坐标 mm；质量 kg；业务时间 UTC

## 当前约定

- `beam_position` 表示最小、唯一、单梁占用的梁场物理梁位。
- `beam.current_position_id` 仅为梁当前所在梁场物理梁位的唯一真源，不代表运输、到场或架设阶段的位置。
- `beam.status` 使用稳定的大写字符串代码，当前代码清单见“梁状态代码”。数据库字段保持 `VARCHAR(32)`，便于后续新增或废弃状态。
- `created_at` 和 `updated_at` 由 ORM 在应用侧写入无时区标记的 UTC 时间。MySQL 字段本身没有服务器默认值。
- `is_active` 的应用默认值为 `TRUE`，但 MySQL 字段没有服务器默认值。
- `beam.status` 的应用默认值为 `UNPRODUCED`，但 MySQL 字段没有服务器默认值。

## 仍待确认

- `area_type` 的允许代码清单。
- `production_date` 的业务口径，例如浇筑日期或成品完成日期。
- 场地业务坐标的原点、轴方向和坐标系版本。
- 一梁一码中的不可变梁身份、二维码和 RFID 标识的数据模型。

## 表关系

| 父表或实体 | 子表或实体 | 关系 | 说明 |
|---|---|---|---|
| `yard_area` | `beam_position` | 1 : N | 一个区域包含多个梁位；每个梁位属于一个区域。 |
| `yard_area` | `yard_area` | 1 : N 可选 | 通过 `parent_id` 支持区域层级；顶层区域 `parent_id` 为空。 |
| `beam_type` | `beam` | 1 : N | 一种梁型可对应多片梁；每片梁对应一个梁型。 |
| `beam_position` | `beam` | 0..1 : 0..1 | 一片梁当前最多占用一个梁位；一个梁位最多被一片梁占用。 |

## `yard_area`

| 字段名 | 中文名称 | 类型 | PK | FK | UNIQUE | 可空 | 数据库默认值 | 应用默认值 | 索引 | 引用 | 说明 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | 主键 | `BIGINT` | 是 |  |  | 否 | — | — | PRIMARY | — | 技术主键，自增。 |
| `area_code` | 区域编码 | `VARCHAR(64)` |  |  | 是 | 否 | — | — | UNIQUE | — | 区域稳定业务编码。 |
| `area_name` | 区域名称 | `VARCHAR(128)` |  |  |  | 否 | — | — | — | — | 区域显示名称。 |
| `area_type` | 区域类型 | `VARCHAR(32)` |  |  |  | 否 | — | — | — | — | 区域类型稳定代码；允许代码仍待确认，不使用 MySQL ENUM。 |
| `parent_id` | 父区域ID | `BIGINT` |  | 是 |  | 是 | — | — | INDEX | `yard_area.id` | 自关联外键；顶层区域为空；删除限制为 RESTRICT。 |
| `sort_order` | 排序值 | `INT` |  |  |  | 否 | `0` | `0` | — | — | 用于展示排序。 |
| `is_active` | 是否启用 | `BOOLEAN` |  |  |  | 否 | — | `TRUE` | — | — | 基础档案启用或停用标志。 |
| `remark` | 备注 | `VARCHAR(500)` |  |  |  | 是 | — | — | — | — | 补充说明。 |
| `created_at` | 创建时间 | `DATETIME(6)` |  |  |  | 否 | — | ORM 写入 UTC | — | — | 创建记录时写入无时区标记的 UTC 时间。 |
| `updated_at` | 更新时间 | `DATETIME(6)` |  |  |  | 否 | — | ORM 写入 UTC | — | — | 创建及更新记录时写入无时区标记的 UTC 时间。 |

## `beam_type`

| 字段名 | 中文名称 | 类型 | PK | FK | UNIQUE | 可空 | 数据库默认值 | 应用默认值 | 索引 | 引用 | 说明 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | 主键 | `BIGINT` | 是 |  |  | 否 | — | — | PRIMARY | — | 技术主键，自增。 |
| `type_code` | 梁型编码 | `VARCHAR(64)` |  |  | 是 | 否 | — | — | UNIQUE | — | 梁型稳定业务编码。 |
| `type_name` | 梁型名称 | `VARCHAR(128)` |  |  |  | 否 | — | — | — | — | 梁型名称。 |
| `length_mm` | 标称长度 | `DECIMAL(12,3)` |  |  |  | 是 | — | — | — | — | 单位 mm；应用层要求非负。 |
| `width_mm` | 标称宽度 | `DECIMAL(12,3)` |  |  |  | 是 | — | — | — | — | 单位 mm；应用层要求非负。 |
| `height_mm` | 标称高度 | `DECIMAL(12,3)` |  |  |  | 是 | — | — | — | — | 单位 mm；应用层要求非负。 |
| `weight_kg` | 标称质量 | `DECIMAL(12,3)` |  |  |  | 是 | — | — | — | — | 单位 kg；应用层要求非负。 |
| `description` | 描述 | `VARCHAR(500)` |  |  |  | 是 | — | — | — | — | 梁型补充描述。 |
| `is_active` | 是否启用 | `BOOLEAN` |  |  |  | 否 | — | `TRUE` | — | — | 基础档案启用或停用标志。 |
| `created_at` | 创建时间 | `DATETIME(6)` |  |  |  | 否 | — | ORM 写入 UTC | — | — | 创建记录时写入无时区标记的 UTC 时间。 |
| `updated_at` | 更新时间 | `DATETIME(6)` |  |  |  | 否 | — | ORM 写入 UTC | — | — | 创建及更新记录时写入无时区标记的 UTC 时间。 |

## `beam_position`

| 字段名 | 中文名称 | 类型 | PK | FK | UNIQUE | 可空 | 数据库默认值 | 应用默认值 | 索引 | 引用 | 说明 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | 主键 | `BIGINT` | 是 |  |  | 否 | — | — | PRIMARY | — | 技术主键，自增。 |
| `position_code` | 梁位编码 | `VARCHAR(64)` |  |  | 是 | 否 | — | — | UNIQUE | — | 最小物理梁位的稳定业务编码。 |
| `position_name` | 梁位名称 | `VARCHAR(128)` |  |  |  | 是 | — | — | — | — | 可读名称，可为空。 |
| `area_id` | 所属区域ID | `BIGINT` |  | 是 |  | 否 | — | — | INDEX | `yard_area.id` | 所属梁场区域；删除限制为 RESTRICT。 |
| `x_mm` | X坐标 | `DECIMAL(14,3)` |  |  |  | 是 | — | — | — | — | 场地业务坐标，单位 mm；坐标原点、轴向和版本仍待确认，不直接绑定 UE5 世界坐标。 |
| `y_mm` | Y坐标 | `DECIMAL(14,3)` |  |  |  | 是 | — | — | — | — | 场地业务坐标，单位 mm；坐标原点、轴向和版本仍待确认。 |
| `z_mm` | Z坐标 | `DECIMAL(14,3)` |  |  |  | 是 | — | — | — | — | 场地业务坐标，单位 mm；坐标原点、轴向和版本仍待确认。 |
| `is_active` | 是否启用 | `BOOLEAN` |  |  |  | 否 | — | `TRUE` | — | — | 梁位启用或停用标志。 |
| `remark` | 备注 | `VARCHAR(500)` |  |  |  | 是 | — | — | — | — | 补充说明。 |
| `created_at` | 创建时间 | `DATETIME(6)` |  |  |  | 否 | — | ORM 写入 UTC | — | — | 创建记录时写入无时区标记的 UTC 时间。 |
| `updated_at` | 更新时间 | `DATETIME(6)` |  |  |  | 否 | — | ORM 写入 UTC | — | — | 创建及更新记录时写入无时区标记的 UTC 时间。 |

## `beam`

| 字段名 | 中文名称 | 类型 | PK | FK | UNIQUE | 可空 | 数据库默认值 | 应用默认值 | 索引 | 引用 | 说明 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | 主键 | `BIGINT` | 是 |  |  | 否 | — | — | PRIMARY | — | 数据库技术主键，自增。 |
| `beam_code` | 梁编号 | `VARCHAR(64)` |  |  | 是 | 否 | — | — | UNIQUE | — | 梁的稳定业务编号；不同于技术主键，也不直接等同于二维码或 RFID 标识。 |
| `beam_name` | 梁名称 | `VARCHAR(128)` |  |  |  | 是 | — | — | — | — | 可读名称，可为空。 |
| `beam_type_id` | 梁型ID | `BIGINT` |  | 是 |  | 否 | — | — | INDEX | `beam_type.id` | 关联梁型；删除限制为 RESTRICT。 |
| `current_position_id` | 当前梁场梁位ID | `BIGINT` |  | 是 | 是 | 是 | — | — | UNIQUE | `beam_position.id` | 当前梁场物理梁位唯一真源；为空表示当前无梁场梁位，不代表其他阶段没有位置；删除限制为 RESTRICT。 |
| `status` | 当前状态 | `VARCHAR(32)` |  |  |  | 否 | — | `UNPRODUCED` | — | — | 保存稳定大写状态代码；当前代码见“梁状态代码”。 |
| `production_date` | 生产日期 | `DATE` |  |  |  | 是 | — | — | — | — | 具体业务口径仍待确认，仅保存日期。 |
| `remark` | 备注 | `VARCHAR(500)` |  |  |  | 是 | — | — | — | — | 补充说明。 |
| `created_at` | 创建时间 | `DATETIME(6)` |  |  |  | 否 | — | ORM 写入 UTC | — | — | 创建记录时写入无时区标记的 UTC 时间。 |
| `updated_at` | 更新时间 | `DATETIME(6)` |  |  |  | 否 | — | ORM 写入 UTC | — | — | 创建及更新记录时写入无时区标记的 UTC 时间。 |

## 梁状态代码

| 状态代码 | 中文名称 |
|---|---|
| `UNPRODUCED` | 未生产 |
| `REBAR_BINDING` | 钢筋绑扎 |
| `REBAR_CHECK` | 钢筋验收 |
| `FORMWORK_CHECK` | 模板验收 |
| `CONCRETE_CASTING` | 混凝土浇筑 |
| `CURING` | 养护 |
| `TENSION_GROUTING` | 张拉压浆 |
| `QUALITY_ACCEPTED` | 成品验收 |
| `STORED` | 存梁 |
| `READY_TO_SHIP` | 待出厂 |
| `TRANSPORTING` | 运输中 |
| `ARRIVED` | 已到场 |
| `ERECTING` | 架设中 |
| `COMPLETED` | 架设完成 |

状态代码允许在后续版本新增或废弃，但不应直接修改已经用于历史数据的代码含义。

## 一梁一码与生命周期扩展边界

当前四张表只覆盖梁场基础档案和当前状态，不等同于完整生命周期模型。

- `beam.id` 是数据库技术主键。
- `beam.beam_code` 是稳定业务梁号。
- 二维码和 RFID 是现场识别媒介，不应直接替代技术主键或业务梁号。
- 是否新增不可变 `beam_uid`，以及二维码、RFID 是否采用独立身份表，需要在手持设备和生命周期模块开发前单独确认。
- `beam.current_position_id` 只表达当前梁场梁位。运输轨迹、到场位置、架设位置和历史移动记录应由后续事件或位置历史模型承担。
- V1 不提供梁删除接口。生命周期历史和审计数据不得通过删除梁实现清理。

## V1 基础设计规范

- **DB-01**：表名采用小写单数 snake_case。
- **DB-02**：ORM 类采用 PascalCase。
- **DB-03**：普通业务表主键统一命名为 `id`。
- **DB-04**：主键默认 `BIGINT AUTO_INCREMENT`。
- **DB-05**：业务编码与技术主键分离；业务编码按需 UNIQUE。
- **DB-06**：外键统一命名为 `<entity>_id`。
- **DB-07**：核心业务外键默认禁止级联删除。
- **DB-08**：字段默认 NOT NULL，只有确有未知或不存在语义时允许 NULL。
- **DB-09**：时间字段统一使用 `*_at` 或 `*_date` 命名。
- **DB-10**：业务时间统一使用 `DATETIME(6)`，数据库内按 UTC 解释。
- **DB-11**：不全局采用软删除；基础档案优先使用 `is_active`。
- **DB-12**：状态字段使用 VARCHAR 保存稳定代码；当前代码清单需版本化维护，不使用 MySQL ENUM。
- **DB-13**：几何尺寸和坐标基础单位为 mm，质量基础单位为 kg。
- **DB-14**：大文件不直接保存到 MySQL。
- **DB-15**：索引根据真实查询场景设计，不机械给所有字段建立索引。
- **DB-16**：默认采用规范化关系设计；同一事实尽量只有一个真源。
- **DB-17**：数据库完整性通过 PK、FK、UNIQUE、NOT NULL 等约束兜底。
- **DB-18**：所有表结构修改通过 SQLAlchemy ORM 和 Alembic 管理，不手工 ALTER 共享数据库。
- **DB-19**：`beam.current_position_id` 仅表示当前梁场物理梁位，不作为运输、到场或架设位置的全局真源。
- **DB-20**：数据库技术主键、梁业务编号、二维码和 RFID 标识必须区分；身份扩展另行设计。
- **DB-21**：V1 不提供梁删除接口；历史与审计数据不得通过删除梁实现清理。
- **DB-22**：数据字典应区分数据库默认值与应用默认值；应用默认不等同于 MySQL DEFAULT。

## 索引说明

当前索引覆盖业务编码、外键和梁位唯一占用约束。后续只有在 B 的真实查询或增量同步方案确认后，才评估增加 `beam(status, id)`、`beam(updated_at, id)` 等索引。

## 模块边界

本数据字典属于 A 数据库模块。A 负责表结构、迁移、ORM、数据访问、数据库约束和数据字典；FastAPI、登录鉴权、Redis、MQTT、WebSocket、扫码设备通信、RFID 设备通信、业务流程编排和 UE5 数据广播均不在本字典和 A 模块实现范围内。

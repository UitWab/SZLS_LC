# V9 审计问题修复开发复测记录

## 1. 结论

V9 审计发现的项目停用并发竞态、默认项目时间线索引缺口和文档同步问题已按最小改动方案修复。业务边界和公开接口保持不变，未新增模块、数据库表或公开方法。

当前结论为“开发侧修复及复测通过，待独立测试与审计复核”，尚未冻结、提交或推送。

## 2. 修复内容

1. 异常事项创建解析项目时使用共享行锁，使新建异常与项目停用按项目行串行；多个异常创建仍可共享读取锁并行执行。
2. 保持停用项目上的相同幂等请求可恢复；只拒绝停用后的新异常。
3. 增加确定性并发回归测试，验证停用事务提交后等待中的新建请求被拒绝且不产生记录。
4. 增加 `ix_abnormal_issue_project_occurred_id (project_id, occurred_at, id)`，支撑默认项目异常时间线。
5. 同步 ORM、V9 迁移、结构测试、设计草案和数据字典。
6. README 更新为 V9 当前迁移和验证状态；A-B 筛选总表补充异常事项。

## 3. 数据库处理

修改前确认开发库和测试库均处于 `a9d3e5f7b102 (head)`，且 `abnormal_issue` 均为 0 行。由于 V9 尚未冻结、提交或推送，两库安全降级至 V8 后完善现有 V9 迁移，再重新升级。

最终状态：

- 开发库：`a9d3e5f7b102 (head)`；
- 测试库：`a9d3e5f7b102 (head)`；
- 两库 `alembic check`：无新增迁移操作；
- 开发库 V9 记录：0；
- 测试库 V9 测试数据残留：0。

## 4. 开发侧验证

| 检查项 | 结果 |
|---|---|
| 新增索引、停用竞态和既有停用幂等定向测试 | `3 passed` |
| V9 专项、结构、接口和架构组合 | `43 passed` |
| 项目停用竞态测试重复执行 | `20/20 passed` |
| 完整回归 | `229 passed in 7.23s` |
| 默认时间线执行计划 | 使用 `ix_abnormal_issue_project_occurred_id`，`Backward index scan; Using index` |
| Wheel 构建和隔离导入 | 通过，102 个条目 |
| Wheel SHA-256 | `6A7C6EFD1AD1C8CF840C00AD4CF49247B428D02A4A5BBE9BAE39D067C4CAE995` |
| 依赖检查、源码编译和差异格式 | 通过 |

## 5. 后续要求

旧测试和审计报告作为历史记录保留，不改写其原始结论。冻结前仍需：

1. 由独立测试任务在当前工作区重新执行迁移、并发、229 项完整回归、wheel 和残留检查，并新增明确的 PASS 报告；
2. 由独立审计任务复核共享锁顺序、索引、文档和独立测试证据；
3. 独立测试及审计均通过后，再执行 V9 冻结、提交、标签和推送。

# V6 审计问题修复开发复测记录

## 1. 结论

最新审计发现的工序归属迁移缺陷、V5 游标事件 ID 类型校验缺陷以及文档和临时目录问题，已按最小改动方案修复并通过开发侧复测。当前状态为“开发复测通过，待独立测试与审计复核”，尚未冻结、提交或推送。

## 2. 修复内容

1. 工序归属变更与新增执行记录统一锁定同一工序行；工序已有执行记录时，禁止实际改变项目归属。
2. 工序名称、排序、备注、启停以及未引用工序的项目归属维护保持原行为。
3. V5 游标事件 ID 改为严格整数校验，拒绝小数、布尔值、字符串、空值、NaN、零和负数。
4. README、V6 暂行设计和 A-B 接口说明补齐规则；本地冻结和 Wheel 验证目录加入精确忽略规则。

没有新增业务模块、公开接口、ORM 字段或 Alembic 迁移。

## 3. 验证结果

| 验证项 | 结果 |
|---|---|
| V5/V6 联合定向测试 | `26 passed` |
| 完整回归 | `186 passed` |
| 工序记录/迁移并发专项 | 连续 20 轮全部通过 |
| Python 编译检查 | 通过 |
| 开发库迁移 | `d4a6f8b0c217 (head)` |
| 模型迁移一致性 | `No new upgrade operations detected.` |
| 历史数据作用域检查 | 不兼容记录 `0` 条 |
| Wheel 构建 | `smart_beam_twin_backend_db-6.0.0-py3-none-any.whl` 构建成功 |
| Wheel 隔离导入 | `BeamProcessExecutionService` 导入成功 |
| Git 差异格式检查 | 通过 |
| 临时目录忽略 | `.tmp_freeze/`、`.v6_test_wheel_*/` 均命中规则 |

## 4. 待复核重点

- 独立重复执行工序归属变更与记录写入的并发测试；
- 确认所有失败路径均回滚且不存在死锁或锁等待超时；
- 确认 V5 合法整数游标仍保持排他增量语义；
- 完整复核 A 只提供数据能力，没有增加 B 的鉴权、排程、HTTP 或页面功能。

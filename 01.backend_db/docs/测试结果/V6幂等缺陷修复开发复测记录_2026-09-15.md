# V6 幂等缺陷修复开发复测记录

## 1. 结论

测试报告中的 `V6-001` 已按最小改动方案修复，并通过开发侧复测。当前状态是“开发复测通过，待独立测试复核”，尚未冻结、提交或推送。

## 2. 修复内容

工序执行记录仍先完成项目、梁、工序、用户和替代记录的存在性及归属解析，但不在幂等识别前拒绝停用项目或工序：

1. 解析稳定业务内容；
2. 查询执行编码和外部幂等键；
3. 既有内容一致时返回原记录，内容不一致时返回冲突；
4. 仅在确实需要插入新记录时检查项目和工序是否启用。

并发唯一冲突恢复使用同一处理顺序。公开 DTO、Service 方法、数据库模型和迁移均未改变。

## 3. 验证结果

| 验证项 | 结果 |
|---|---|
| V6 联合定向测试 | `38 passed` |
| 完整回归 | `183 passed` |
| 并发创建与并发作废 | 连续 10 轮，共 20 次用例全部通过 |
| 测试库迁移 | `d4a6f8b0c217 (head)` |
| 模型迁移一致性 | `No new upgrade operations detected.` |

新增正式自动测试覆盖：停用工序后的编码与外部键重试、停用项目后的重试、停用资源下不同内容冲突、新记录拒绝、通过公开 `record` 路径触发的并发冲突恢复，以及 V6 表字段、外键、唯一约束、检查约束和索引。

## 4. 边界确认

- 相同请求返回历史记录不属于新增写入，因此不受当前启用状态影响；
- 停用资源仍禁止创建新记录；
- 跨项目、资源不存在和内容冲突检查没有取消；
- 没有增加生产流程、鉴权或状态联动功能；
- 原失败报告保留，最终冻结判断应以独立修复复测报告为准。

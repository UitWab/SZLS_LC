# 智能建梁数字孪生平台数据库模块 V5 并发游标修复复测交接文档

## 一、复测结论

- 复测日期：2026-09-14
- 时区：Asia/Shanghai
- 测试对象：`dev-db` 分支当前未提交的 V5 最新工作区
- 复测结论：**通过**
- V5 联合定向测试：`32 passed`
- 全量回归测试：首次及最终均为 `168 passed`
- 关键并发用例重复测试：连续 10 轮、共 20 次用例全部通过
- 同项目持锁事务回滚恢复：通过
- 最新迁移往返：通过
- Python 包构建：通过
- 阻塞缺陷：未发现

本次复测针对首次 V5 测试后新增的并发游标修复。实现已将增量水位从 `(occurred_at, id)` 调整为事件 `id`，增加项目级事件流锁、项目增量索引和版本 2 游标，并新增同项目串行、跨项目并行及旧游标拒绝用例。复测结果表明这些变化有效，且 V1 至 V4 回归未受影响。

本文是 `V5测试结果交接文档_2026-09-14.md` 的后续复测结果；涉及当前 V5 最终状态时，以本文为准。

## 二、复测基线

| 项目 | 实际值 |
| --- | --- |
| 项目目录 | `D:\Project\SZLS_LC\01.backend_db` |
| Git 分支 | `dev-db` |
| HEAD | `c8f6f109966ccb146c7f13fcbca03f43afdeb17d` |
| HEAD 标签 | `backend-db-v4.0.0` |
| V5 工作区 | 已开发、未提交、未冻结 |
| Python | `3.11.9` |
| Pytest | `9.1.1` |
| MySQL | `8.0.46` |
| 包版本 | `5.0.0` |
| V5 事件表迁移 | `b7e1c3d5f902` |
| V5 最新迁移 | `c8f2d4e6a013` |
| 测试库 | `smart_beam_twin_test` |
| 开发库 | `smart_beam_twin_dev` |

本次测试开始时工作区已有 17 项已跟踪文件修改和 12 项未跟踪文件。测试保留全部开发改动，不执行重置、清理、提交或推送。

## 三、资料与验收口径

本次重新阅读并核对：

1. `README.md`；
2. `docs/V5梁生命周期事件设计决策.md`；
3. `docs/A-B接口接入说明.md` 中 V5 契约；
4. `D:\Project\SZLS_LC\00.docs\数据库数据字典\智能建梁数字孪生平台_V5_数据库数据字典_梁生命周期事件域.md`；
5. 新增迁移、模型、CRUD、Service 和 V5 自动测试。

README、设计决策、A-B 接口说明和数据字典现在一致约定：

- 增量读取按事件 ID 升序；
- 游标版本为 2，仅保存事件 ID；
- 同项目或全局范围的事件写入串行化；
- 不同项目的事件写入互不阻塞；
- 新增 `(project_id, id)` 索引；
- `beam_lifecycle_event_stream_lock` 是内部锁表，不向 B 暴露。

项目中仍未找到 V5 专属测试任务交接文档，本次继续以上述四份资料共同作为验收基线。

## 四、变更核对

首次测试后确认新增或调整：

| 变更 | 核对结果 |
| --- | --- |
| `c8f2d4e6a013_add_event_stream_lock.py` | 新增内部锁表和 `(project_id, id)` 索引 |
| `BeamLifecycleEventStreamLock` | `scope_key` 为非空字符串主键，无外键 |
| 事件创建 CRUD | 写事件前按 `PROJECT:<id>` 或 `GLOBAL` 获取事务锁 |
| 增量查询 | 改为 `id > after_id` 并按 ID 升序 |
| 游标格式 | 版本 2，URL-safe Base64，错误版本被拒绝 |
| 自动测试 | 新增同项目提交顺序、跨项目并行和内部表结构测试 |
| 文档 | README、设计决策、A-B 说明和数据字典已同步最新口径 |

## 五、自动测试结果

### 5.1 环境检查

| 检查项 | 结果 | 输出 |
| --- | --- | --- |
| Python | 通过 | `Python 3.11.9` |
| 依赖完整性 | 通过 | `No broken requirements found.` |
| 源码编译 | 通过 | 无错误输出 |
| 测试库安全隔离 | 通过 | 显式使用 `smart_beam_twin_test` |

### 5.2 V5 联合定向测试

测试范围：

- `backend_db/tests/test_beam_lifecycle_event.py`；
- `backend_db/tests/test_schema.py`；
- `backend_db/tests/test_interfaces.py`；
- `backend_db/tests/test_architecture.py`。

结果：

```text
32 passed in 1.27s
```

### 5.3 全量回归

首次全量结果：

```text
168 passed in 3.19s
```

迁移往返和补充验证后的最终结果：

```text
168 passed in 3.43s
```

没有失败、错误、跳过或预期失败用例。

## 六、并发与游标专项验证

| 场景 | 结论 | 结果说明 |
| --- | --- | --- |
| 同项目并发写入 | 通过 | 第二个写事务等待第一个事务结束，不能越过未提交事件 |
| 提交后增量读取 | 通过 | 两条事件按 ID 升序完整返回，无重复、无遗漏 |
| 跨项目并发写入 | 通过 | 第二项目写入不等待第一项目事务 |
| 相同时间戳 | 通过 | `occurred_at` 相同不影响按 ID 连续读取 |
| 排他游标 | 通过 | 游标对应事件不重复返回 |
| 空批次游标 | 通过 | 保留传入游标，`has_more=False` |
| 损坏游标 | 通过 | 转换为稳定的 `InvalidDataError` |
| 旧版本游标 | 通过 | 版本 1 游标被拒绝，符合部署重置约定 |
| 持锁事务回滚 | 通过 | 等待事务随后成功提交，失败梁和失败事件均不可见 |
| 锁释放后的恢复 | 通过 | 同一项目后续事件写入与查询正常 |

两个正式并发测试连续运行 10 轮，结果均为：

```text
2 passed
```

累计 20 次并发用例全部通过，无超时、死锁或偶发失败。

补充的持锁回滚测试使用不落盘脚本模拟第一个事务取得项目锁并插入事件后失败，确认第二个同项目事务先等待、再于前一事务回滚后成功提交。补充数据已全部清理。

## 七、其他 V5 功能回归

以下首次测试能力在本次全量回归中继续通过：

- 五类生命周期事件及字段形状；
- 状态和梁位幂等操作不产生事件；
- 普通梁资料更新不产生事件；
- PLACE、MOVE、RELEASE 工单完成只产生一条关联事件；
- 工单创建、开始和取消不产生梁生命周期事件；
- 业务失败、事件写入失败和提交失败整体回滚；
- 项目隔离及全局梁兼容；
- 页码分页、筛选和稳定排序；
- 公开接口只读，不暴露事件创建、更新或删除；
- DTO 与架构依赖边界；
- V1 至 V4 全量回归。

## 八、数据库结构与迁移

### 8.1 最新结构

- `beam_lifecycle_event` 保持 11 个业务字段和 5 个 `RESTRICT` 外键；
- 原有 `(project_id, occurred_at, id)`、`(beam_id, occurred_at, id)` 索引保留；
- 新增 `(project_id, id)` 增量查询索引；
- 新增 `beam_lifecycle_event_stream_lock`；
- 锁表仅有非空主键 `scope_key`，没有外键和业务数据字段。

### 8.2 最新迁移往返

1. 测试库初始为 `c8f2d4e6a013 (head)`；
2. 成功降级到 `b7e1c3d5f902`；
3. 降级后事件表保留，锁表和 `(project_id, id)` 索引移除；
4. 成功升级回 `c8f2d4e6a013 (head)`；
5. 升级后锁表、主键和增量索引正确恢复；
6. 恢复后 `alembic check` 无模型差异；
7. 恢复后全量 168 项测试通过。

### 8.3 最终数据库状态

| 数据库 | 最终迁移 | 检查结果 | 写入说明 |
| --- | --- | --- | --- |
| `smart_beam_twin_test` | `c8f2d4e6a013 (head)` | 无模型差异 | 执行自动测试、补充测试和迁移往返 |
| `smart_beam_twin_dev` | `c8f2d4e6a013 (head)` | 无模型差异 | 仅执行 current/check 和版本读取 |

开发库未执行降级、清空、重建或业务数据写入。

## 九、Python 包构建

| 项目 | 实际值 |
| --- | --- |
| 文件名 | `smart_beam_twin_backend_db-5.0.0-py3-none-any.whl` |
| 大小 | 94,750 字节 |
| SHA-256 | `b3f72d112b7488d0da4fd99e23799ddc1641b040913a53ede8619e9826f60cf1` |
| 结果 | 通过 |

已确认 wheel 包含事件 CRUD、模型、查询 Service、内部事件记录模块，以及 `b7e1c3d5f902`、`c8f2d4e6a013` 两个 V5 迁移文件。测试用 wheel 临时目录已清理。

## 十、残留与工作区

最终残留检查：

```text
event_projects=0
event_beams=0
event_work_orders=0
event_rows=0
```

- 没有遗留测试项目、梁、工单或生命周期事件；
- 补充脚本未落盘；
- wheel 临时目录已删除；
- 测试未修改业务源码、迁移或既有测试；
- 本文档是本次复测新增的持久化产物；
- 未提交、未推送。

## 十一、缺陷与建议

本次未发现可复现的功能缺陷、并发缺陷、游标缺陷、迁移缺陷、回归缺陷或包构建缺陷。

仍有一个非阻塞资料项：项目中没有 V5 专属测试任务交接文档。建议冻结前补齐该文档，并由独立代码审计重点复核事务锁范围、潜在死锁顺序、游标跨筛选范围使用约束和部署时旧游标重置流程。

## 十二、冻结建议

V5 最新并发游标修复已通过定向、全量、重复并发、回滚恢复、迁移往返、结构一致性和包构建验证，当前没有阻塞冻结的问题。

建议补齐 V5 测试任务交接资料并完成独立代码审计后冻结 V5 候选版本。正式提交、打标签和推送由主开发任务执行，本测试任务不执行这些操作。

# V7 本地冻结记录

## 1. 冻结结论

V7 质量检查记录以 `7.0.0` 基线本地冻结，目标迁移为 `e7b9c1d3f605`，冻结标签为 `backend-db-v7.0.0`。本次不推送远端。

## 2. 冻结范围

- 质量检查主记录和不可变检查项快照；
- 项目和梁作用域、可选工序及 V6 执行记录关联；
- 同梁复检关系、外部幂等、受控作废、筛选和页码分页；
- 区分大小写的精确 `item_code` 契约及顺序无关幂等比较；
- ORM、Alembic 迁移、DTO、CRUD、Service、公开 Protocol、组合工厂、测试、数据字典和 A-B 接入说明。

不包含 B 的 HTTP、登录鉴权、岗位权限、页面、质量审批、签章、附件、放行、运输或设备功能。

## 3. 冻结前验证

| 验证项 | 结果 |
|---|---|
| V7 定向测试 | `15 passed` |
| 完整自动测试 | `202 passed` |
| 并发创建和并发作废 | 连续 20 轮，共 40 次用例通过 |
| 开发库与测试库迁移 | `e7b9c1d3f605 (head)` |
| 模型与迁移一致性 | 两库均无差异 |
| 质量主表和检查项残留 | 开发库、测试库均为 `0` |
| Wheel 构建与隔离导入 | 通过 |
| 源码编译和 Git 差异格式 | 通过 |

## 4. 审计说明

冻结前独立审计发现了检查项顺序导致的幂等错误、DTO 与 MySQL 编码相等规则不一致，以及列表额外加载检查项三项问题。三项均已按最小改动方案修复，并完成开发侧定向及完整复测。

当前没有修复后的新独立审计报告。用户于 2026-09-16 明确授权冻结；后续复核若发现缺陷，应以新提交和向前迁移修复，不改写冻结标签。

## 5. 后续规则

- `backend-db-v7.0.0` 标签创建后不可移动；
- V8 从本基线继续，使用新版本和向前迁移；
- V8 只设计运输交接记录，不开发运输计划、车辆档案、实时轨迹、电子围栏或 B 模块功能；
- 推送远端必须另行授权。

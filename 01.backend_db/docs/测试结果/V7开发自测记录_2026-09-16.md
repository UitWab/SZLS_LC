# V7 质量检查记录开发自测记录

## 1. 当前结论

V7 已按确认的设计草案完成开发，并已修复独立审计发现的检查项幂等、编码排序规则和列表额外加载问题。当前仅代表开发侧修复及复测通过，尚未完成修复后独立复审、冻结、提交或推送。

## 2. 已实现

- 质量检查主记录和检查项模型及迁移；
- 主从原子创建、项目隔离、V6 执行关联和复检；
- 编码及外部来源幂等、受控作废；
- 筛选、稳定页码分页、DTO 和公开接口；
- 工序被质量记录引用后的项目归属保护；
- 数据字典、A-B 接入说明和测试交接资料。

## 3. 首轮结果

| 项目 | 结果 |
|---|---|
| V7 定向测试 | `14 passed` |
| 首轮完整回归 | `201 passed` |
| 开发库迁移 | `e7b9c1d3f605 (head)` |
| 测试库迁移 | `e7b9c1d3f605 (head)` |
| 迁移降级再升级 | `e7b9c1d3f605 → d4a6f8b0c217 → e7b9c1d3f605` 通过 |
| 模型迁移一致性 | `No new upgrade operations detected.` |
| 并发创建与并发作废 | 连续 20 轮，共 40 次用例通过 |
| Wheel 构建 | `smart_beam_twin_backend_db-7.0.0-py3-none-any.whl` 成功 |
| Wheel 隔离导入 | `BeamQualityInspectionService` 导入成功 |

上述结果均为开发侧验证，最终结论仍以独立测试和审计为准。

## 4. 审计问题修复

1. 幂等比较改为以精确 `item_code` 为键的业务字段映射比较，不再依赖 Python 与 MySQL 的排序结果。
2. `item_code` 明确为区分大小写的精确编码，ORM 与 V7 迁移均使用 `utf8mb4_bin`，`ITEM` 与 `item` 可作为两个不同检查项。
3. 列表查询关闭检查项预加载；`get`、幂等恢复和作废等需要完整记录的路径仍加载检查项。

## 5. 修复后开发复测

| 项目 | 结果 |
|---|---|
| V7 定向测试 | `15 passed` |
| 完整回归 | `202 passed` |
| 混合大小写及反序幂等 | 通过 |
| 精确大小写编码 | `ITEM` 与 `item` 可同时存在，通过 |
| 列表查询 SQL 观测 | 未查询 `beam_quality_inspection_item`，通过 |
| 并发创建与并发作废 | 连续 20 轮，共 40 次用例通过 |
| 开发库与测试库迁移 | `e7b9c1d3f605 (head)` |
| 两库字段排序规则 | `beam_quality_inspection_item.item_code = utf8mb4_bin` |
| 模型迁移一致性 | 两库均为 `No new upgrade operations detected.` |
| Wheel 构建与隔离导入 | 通过 |
| 开发库与测试库残留 | 主表、检查项表均为 `0` |

修复未新增模块、公开方法或 B 模块功能，公开 DTO 和 Service 调用形式保持不变。

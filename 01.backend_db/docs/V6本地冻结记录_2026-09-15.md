# V6 本地冻结记录

## 1. 冻结结论

V5 梁生命周期事件与 V6 梁工序执行记录作为同一个 `6.0.0` 基线本地冻结，目标迁移为 `d4a6f8b0c217`，冻结标签为 `backend-db-v6.0.0`。本次不推送远端。

之所以合并冻结，是因为仓库正式基线仍停留在 V4，V5 与 V6 在同一未提交工作区连续开发。为保持提交内容真实，不创建一个实际包含 V6 代码的 V5 标签。

## 2. 冻结范围

- V5 梁创建、状态变化和梁位变化生命周期事件；
- V5 页码查询与按事件 ID 的游标增量查询；
- V5 同项目事件写入串行保护；
- V6 已结束梁工序执行事实、筛选、分页、幂等和受控作废；
- V6 纠错替代关系及工序项目归属保护；
- ORM、迁移、DTO、CRUD、Service、公开 Protocol、组合工厂、测试、数据字典和接入文档。

不包含 B 的 HTTP、登录鉴权、岗位权限、页面、生产排程、审批、设备控制、消息投递或 UE5 功能。

## 3. 冻结前验证

| 验证项 | 结果 |
|---|---|
| 完整自动测试 | `186 passed` |
| 工序记录/归属迁移并发专项 | 连续 20 轮通过 |
| 数据库迁移 | `d4a6f8b0c217 (head)` |
| 模型与迁移一致性 | `No new upgrade operations detected.` |
| 历史执行记录作用域检查 | 不兼容记录 `0` 条 |
| Wheel 构建与隔离导入 | 通过 |
| Git 差异格式检查 | 通过 |

## 4. 审计说明

冻结前最近一次独立审计发现的工序项目迁移、V5 游标整数校验以及文档和临时目录问题均已修复，并完成开发侧复测。用户于 2026-09-15 明确授权冻结。

当前没有修复后的新独立审计报告。后续复核若发现缺陷，应通过新提交修复，不改写或移动冻结标签。

## 5. 后续规则

- `backend-db-v6.0.0` 标签创建后不可移动；
- 后续变更从该基线继续，以新版本和向前迁移交付；
- V7 只开发质量检查记录，不把运输、告警、设备或 B 模块功能带入同一版本；
- 推送远端必须另行授权。
